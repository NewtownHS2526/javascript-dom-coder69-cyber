// TODO: Use template literals with ${} to insert variables
// Example: `${firstName} is ${age} years old`

let firstName = "fayejul";
let age = 15;
let message = `${firstName} is ${age} years old`;
console.log(message);